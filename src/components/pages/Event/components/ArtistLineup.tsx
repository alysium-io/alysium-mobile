import { View } from '@atomic';
import React from 'react';

const ArtistLineup = () => {
	return (
		<View>{/* <Lineup lineup={global.sampleData.event.lineup} /> */}</View>
	);
};

export default ArtistLineup;
