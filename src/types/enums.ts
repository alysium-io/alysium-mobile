export enum ContentType {
    user = 'user',
    artist = 'artist',
    tag = 'tag',
    host = 'host',
    event = 'event',
    location = 'location'
}

export enum EventStatus {
    live = 'live',
    draft = 'draft',
    published = 'published',
    cancelled = 'cancelled',
    completed = 'completed'
}