export enum Persona {
	artist = 'artist',
	host = 'host',
	user = 'user'
}

export enum ContentType {
	user = 'user',
	artist = 'artist',
	host = 'host',
	tag = 'tag',
	event = 'event',
	location = 'location'
}
