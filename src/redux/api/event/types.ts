export enum EventStatus {
	draft = 'draft',
	published = 'published',
	canceled = 'canceled'
}

export enum ComplexEventStatus {
	draft = 'draft',
	published = 'published',
	canceled = 'canceled',
	coming_up = 'coming_up',
	live = 'live',
	archived = 'archived'
}
